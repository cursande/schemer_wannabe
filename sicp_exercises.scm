;; I'll organise these notes better once I've built them up a bit

;; Procedure Definitions

; General form of a procedure def: (define (<name> <formal parameters>) <body>)

; Squaring ->
(define (square x) (* x x))
; (square 5) -> 25
